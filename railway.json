{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "providers": ["python"]
    },
    "buildCommand": "pip install -r requirements.txt && python manage.py migrate"
  },
  "start": {
    "cmd": "gunicorn PPM_BackEnd_Project.wsgi:application --bind 0.0.0.0:$PORT"
  }
}